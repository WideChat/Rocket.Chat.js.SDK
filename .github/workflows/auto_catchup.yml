name: Upstream catchup from Rocket.Chat.js.SDK multiple-clients

on:
  schedule:
    - cron: '0 0 * * 1' #on every Monday

jobs:
  catchup:
    name: Upstream Catchup
    runs-on: ubuntu-latest

    steps:

    - name: Catchup Info
      run: |
        echo "GITHUB_ACTION: $GITHUB_ACTION"
        echo "GITHUB_ACTOR: $GITHUB_ACTOR"
        echo "GITHUB_REF: $GITHUB_REF"
        echo "github.event_name: ${{ github.event_name }}"
        cat $GITHUB_EVENT_PATH
    - name: Checkout
      uses: actions/checkout@v2
    - name: Set up Node
      uses: actions/setup-node@v1
      with:
        node-version: 12
    - name: Create Catchup PR
      uses: shubhsherl/create-catchup-pr@v0.1.0
      with:
        GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        SOURCE_BRANCH: "multiple-clients"
        SOURCE_REPO: "RocketChat"
        TARGET_BRANCH: "multiple-clients"
        GITHUB_REPO: ${{github.repository}}
        TITLE: "[Upstream Catchup] Merge Rocket.Chat.js.SDK:multiple-clients to multiple-clients"
        BODY: "Weekly Catchup PR to merge Rocket.Chat.js.SDK:multiple-clients in multiple-clients."
        DRAFT: "true"
